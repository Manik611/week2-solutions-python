N = int(input("Enter size of matrix (N): "))
A = [list(map(int, input().split())) for _ in range(N)]

count = 0
row, col = 0, N - 1
while row < N and col >= 0:
    if A[row][col] == 0:
        count += col + 1
        row += 1
    else:
        col -= 1

print("Total number of zeros:", count)
